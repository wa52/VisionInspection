# VisionInspection

WPF (`net8.0-windows` + x64) 扬声器产线视觉检测平台：Recipe 方案驱动的节点式检测流水线 + 海康相机硬触发 → 检测 → OK/NG → 相机 IO 电平回传 PLC。UI 全中文、深色主题（`Theme/Dark.xaml`）；交互在 `MainWindow` partial 组（2026-09-06 起 code-behind 按职责拆分，无 designer）：`MainWindow.xaml.cs`（字段/构造/命令/日志/退出）+ `.Execution.cs`（执行入口/按键触发/硬触发驱动装配）+ `.Recipe.cs`（方案读写/锁定/编辑历史）+ `.NodeEditor.cs`（节点树/参数面板/打分）+ `.RoiOverlay.cs`（ROI 绘制/叠加层）+ `.Results.cs`（结果渲染/状态面板/触发回显）+ `.Devices.cs`（相机/通信弹窗接线）——改 UI 先找对应 partial，控件改动保留中文文案与既有 AutomationId 习惯。

## Commands

- `dotnet build .\VisionInspection.csproj`（强制 x64；构建不需要任何环境变量）
- `dotnet test .\VisionInspection.Tests\VisionInspection.Tests.csproj`（xunit，~15s，全离线可跑，无真实相机依赖）
- 单测过滤：`dotnet test ... --filter "FullyQualifiedName~YoloNodeTests"`；分类过滤 Performance/Stability 用 `[Trait("Category", ...)]` 标注，如 `dotnet test --filter "Category=Performance"`
- 运行：构建后直接启动 `bin\Debug\net8.0-windows\VisionInspection.exe`（或仓库根的 `启动视觉检测平台.lnk`）；启动即自检并加载 exe 同目录 `recipe.json`
- 未配置任何 lint/格式化工具（无 .editorconfig/analyzer 配置），依赖 csproj 的 `Nullable`/`ImplicitUsings` 与既有代码风格

## 依赖兄弟项目 HikCameraManager

- csproj 以 `Exists(...)` 条件引用 `..\HikCameraManager\sdk\managed\MvCameraControl.Net.dll`（缺失则编译失败，无回退），并把 `..\HikCameraManager\sdk\native\**` 原生运行时复制进输出目录。移动/删除 HikCameraManager 会破坏本项目构建。
- `Camera/`、`Trigger/`、`Plc/` 是从 HikCameraManager 拷来的 vendored 副本（仅 namespace 不同；`TriggerStateMachine` 额外多了 `ProcessingError`）。不要"重构"成项目引用；修 bug 时先判断是否需要镜像回 HikCameraManager。

## 配置加载（启动即自检）

- 启动时 `EnvironmentCheckService` 自检：x64 进程、随程序发布的 MVS 托管 DLL 和相机原生 DLL，缺失会给出中文可操作提示；不检查 VisionMaster、MVDAlgorithmSDK、`vServerApp.exe` 或加密狗。
- SDK 路径解析（`VisionMasterConfigResolver`）：与 exe 同目录的 `appsettings.json`（复制自 `appsettings.example.json`，允许 JSON 注释）只读取 `MVS_RUNTIME_DIR`/`mvsRuntimeDir`；未配置时回退到应用目录内置副本。检测算法使用本项目的 ONNX Runtime/OpenCV，不依赖 VisionMaster 服务。
- 方案持久化为 exe 同目录 `recipe.json`（自动加载/保存上次方案）；旧 `config.json` 首次运行自动迁移。产线脚本依赖 `DEPLOY_INPUT_DIR` / `DEPLOY_RESULT_DIR` / `DEPLOY_VM_HOST` / `DEPLOY_VM_PORT` / `DEPLOY_MODEL_DIR` 环境变量覆盖（`DEPLOY_MODEL_DIR` 覆盖第一个 PatchCore 节点）——不要动 `RecipeStore.ApplyEnvOverrides`。跑测试前确保这些环境变量未设置（`RecipeStoreTests` 假定干净状态）。

## 顶栏（菜单栏 + 工具栏，海康式分离）

- 菜单栏：仅「文件」菜单（新建/打开/保存/方案另存为/退出），Ctrl+N/O/S 走 ApplicationCommands 的 `CommandBinding` 复用工具栏同一批 Click 处理器；后续按需补「设置/工具/系统」菜单（只放已有功能）。
- 工具栏图标全部是矢量 `Path`（无图片资源，样式 `ToolBarIconBtn`）：新建/打开/保存/另存为 ‖ 撤销(Ctrl+Z)/重做(Ctrl+Y) ‖ 锁定 ‖ 相机管理（打开 `CameraManagementDialog`） ‖ 通信管理（打开 `CommManagementDialog`） ‖ 方案名下拉 ‖ 单次/连续执行（2026-09-05 起主工具栏**不再有**相机快捷按钮组 刷新/连接/断开/预览/停止预览/软触发——设备级相机操作只在相机管理弹窗；左栏仍保留相机列表 ListBox 供弹窗引导枚举）。单次=圆圈播放、连续=循环箭头；连续运行中 `UpdateButtonState` 把图标切成停止方块并换深红底（`RunContinuousIcon.Data` 切换）。**左栏「相机列表」面板已删（2026-09-05）**：相机枚举/连接/预览/触发只在相机管理弹窗（打开时主窗口枚举一次作弹窗初始列表，弹窗内可重新刷新）；左栏只剩生产状态 + 检测流程两组。`_selectedCamera` 仅由弹窗连接回调写入（下次打开预选用）。**App.xaml `ShutdownMode="OnMainWindowClose"`**：主窗口关闭即进程退出（防非模态 `NodeParamDialog.Show()` 残留桌面）。`Window_Closed` 释放顺序：硬触发驱动检测（`_triggerDriven`）最先停止 → 相机（后台线程 + 5s 超时，防 UI 同步等待死锁）→ `RecipeExecutionService.Dispose`；控制器内 `DisconnectAsync/SetTriggerModeAsync` 的 await 全部 `ConfigureAwait(false)`（设备层无 UI 亲和——曾因预览激活时关闭软件：await 回投 UI 上下文死锁 → 相机不释放 + 进程挂死）。
- **通信管理弹窗**（`CommManagementDialog`，设备级，VM 通信管理式）：通信设备列表（添加/删除）+ 通信参数（设备名称/协议类型/目标IP/端口/串口参数/自动重连/接收结束符，按协议显隐）+ 连接/断开/保存设备 + 发送测试 + 接收日志（上限 200 条）。设备清单持久化 exe 同目录 `comm.json`（`Comm/CommDeviceStore`）。协议四通道**全部可用**（`Comm/ICommLink` + `CommLinkFactory`，环回单测 `CommLinkTests` 覆盖）：TCP客户端（5s 超时，掉线按 AutoReconnect 3s 重试）、TCP服务端（监听本机端口等 PLC 接入，单客户端服务、断开后回监听、HasPeer 才可发送）、UDP（绑定本地端口收包，目标IP 空=回发最近来包对端）、串口（`System.IO.Ports`，COM名/波特率/数据位/校验/停止位）；仅 ModBus通信 为占位（提示暂未支持，真实 PLC 协议待品牌确认）。接收结束符转义 `\r\n`/`\n`/`\t`，非空按行回调、空按收包块回调；结束符**同时匹配真实控制符与字面转义文本**（如输入框填 `\r\n` → 真实 CRLF 与 4 字符文本 `\r\n` 都切行——对端手打转义发送的场景，`LineAccumulator` 双分隔符）。**空闲刷新（2026-09-08 起，四链路收编 `CommLinkBase.ReceiveChunk`）**：设了结束符但对端 500ms 内不发结束符时，缓冲内容按收包块照常回调——对端（如海康 VM 发送数据，默认无结束符）不按约定也能收到，不再永远缓冲不显示；回调顺序=收包顺序（链路线程与刷新定时器经 `_flushSync` 串行），Stop 时丢弃缓冲。**每设备独立链路（2026-09-08 起，`_connected` 列表按设备引用持有）**：多个设备可同时连接（如 服务端+客户端 对联自测），切换设备选中项不断链；「断开」只断当前选中设备，关窗全断；重复点「连接」=按新参数重连。**设备行连接开关**：列表 ItemTemplate = ToggleButton（文案 连接/断开 随状态）+ 名称（协议类型）；拨动即连/断该设备（`DeviceSwitch_Checked/Unchecked` → `ConnectDeviceCoreAsync(device)` 与「连接」按钮共用，拨开关自动选中该设备使参数面板跟随），开关状态由 `MarkConnected` 写 `DeviceItem.IsConnected`（绑定翻转触发的 Checked/Unchecked 事件有 `LinkOf` 守卫防重入，连接失败自动回弹）；**选中态明显区分**：设备列表选中行 深蓝底+亮蓝描边（`ApplyDarkListStyle(accentSelection: true)`）+ 名称加粗（模板内 DataTrigger 绑 ListBoxItem.IsSelected），参数面板顶部有「参数设备：名称（协议类型）」指示条（`UpdateParamDeviceHeader`，选中变更/实时改名/连接或保存写回参数后跟随刷新）；**设备名实时改名**：`CommDevice` 实现 `INotifyPropertyChanged`（Name/Protocol → 同时通知计算属性 `Display`="名称（协议类型）"，`[JsonIgnore]` 不落盘），「设备名称」输入框 TextChanged 实时写回选中设备 → 列表即时改名并显示协议类型；「保存设备」只落盘不再重建列表（旧实现经 RefreshDeviceList→SelectionChanged→DisposeLink 会误断活动链路）。**协议方向提示**（协议下拉下灰字）+ **连接状态回显状态栏**（StatusChanged/LinkClosed 经 `SetStatus` 调度），TCP客户端 目标IP 为空直接拒绝连接并提示「要被对端接入请改选 TCP服务端」；TCP服务端 绑定 0.0.0.0 全部网卡（VM 同语义），监听状态列出本机全部可用 IPv4（`TcpServerLink.LocalIpv4Summary`，对端接任意本机 IP 均可），服务端模式下隐藏用不到的「目标IP」输入框。链路归弹窗所有（关闭即断开），**通信弹窗是纯联调工具，不参与生产闭环**（生产收发走方案的发送/接收数据节点 + `CommRuntimeService`）；软件单次/连续执行运行中（`_execution.IsRunning`）拒绝打开，硬触发驱动检测中允许打开（切触发模式即自动停检测）。**与方案节点互斥守卫（2026-09-07 起）**：弹窗 `ConnectAsync` 连接前查 `_commRuntime.IsHeld(设备名)`（MainWindow 构造弹窗时传入运行时实例）→ 已被发送/接收数据节点持有的设备拒绝连接并提示。
- **相机管理弹窗**（`CameraManagementDialog`，设备级，对齐 VM 常用参数）：相机列表/刷新/连接/断开/预览/软触发 + 采集参数（曝光模式/曝光/增益模式/增益/Gamma/图像宽/高/帧率/实际帧率只读回显/像素格式）+ 触发参数（模式/Line0 输入沿/延迟/滤波/条件触发数 BurstFrameCount/取图超时）+ **IO 输出（设备控制）**组（IO选择项 Line1/2/3、IO模式 Strobe、反相输出 → `io_communication.json` 持久化，连接/「应用 IO 输出配置」时经 `ConfigureIoCommunicationAsync` 下发硬件 LineSelector/LineMode/LineSource/LineInverter；「相机IO通信」节点执行时按**节点参数**重新下发，节点配置优先于相机管理初始状态），复用主窗口同一 `ICameraController` 句柄（不建第二连接）；`CameraParameters` 新增 ExposureAuto/GainAuto/ImageWidth/ImageHeight（旧 camera.json 缺字段回退 Off/0=不变），`ApplyParametersLocked` 里自动模式先于数值设置（自动生效时数值节点只读）、设帧率前先 `AcquisitionFrameRateEnable=true`；连接后自动应用 `camera.json`/`trigger.json`/`io_communication.json` 已存参数（主窗口 `ApplyStoredCameraSettingsAsync` 与弹窗 `ConnectAsync` 两处；**2026-09-05 起主窗口这份已随工具栏相机按钮组一起删除**，只剩弹窗 `ConnectAsync`），应用后经回调落盘，连接/应用后回读 `ResultingFrameRate`（`ICameraController.ResultingFrameRate`，本仓库唯一实现无需改 Fake）；软件单次/连续执行运行中（`_execution.IsRunning`）按钮全部拒绝操作，**硬触发驱动检测中允许打开弹窗——把触发模式切离硬触发即自动停止检测**（这是切模式入口，不能拦）。主窗口右侧**没有**相机/触发 Tab——设备级相机配置只在此弹窗。
- 方案编辑历史（撤销/重做）：`RecipeEditHistory` 纯逻辑类（根目录，有单测）持有快照栈；MainWindow 持 `_editHistory` + `_restoringSnapshot` 守卫。惰性捕获语义不变：基线是"上个快照点"状态，`PushUndoSnapshot` → `_editHistory.Push(SnapshotRecipe())` 去重、上限 50 步、新撤销点清重做栈。钩子点：`HotApplyParam` 入口 + AddNode/删除节点/RenameNode/MoveNode；新建/打开方案 `ResetEditHistory` → `_editHistory.Reset`。恢复 = `TryUndo/TryRedo` 取快照 → `RestoreRecipeSnapshot` 反序列化替换 `_recipe` → 关掉绑定旧节点实例的弹窗（`_roiManager`/`_roiDialog`）→ `RefreshNodeTree` 统一失效 → `_editHistory.Rebaseline`。
- 方案锁定（`BtnLock`，`_uiLocked`，运行时状态不持久化）：`EnsureUnlocked(what)` 守卫拦 ROI 几何与节点结构修改（WriteOwnRois/WriteOwnRoisFull/SetScopeIndex/SetRoiMeta/AddOwnRoi/RenameOwnRoi/DeleteOwnRoi/AddNode/删除节点/RenameNode/MoveNode/ToggleRoiDraw/StartRoiRedraw 入口），锁定时 `RoiCanvas.IsHitTestVisible` 同步禁用；**参数数值仍可调**（与 VM 锁语义一致）。
- ToolTip 深色样式已重写模板（`Theme/Dark.xaml`，防默认白底白字）——新增悬停提示不用再处理颜色。

## 执行编排（应用层）

- `RecipeExecutionService`（根目录，有单测）：方案执行的编排者——拥有 Pipeline 生命周期（`PrepareAsync` 脏检查后台重建 + 注入图像源 FrameProvider + @input 引用校验）与运行方式（`RunOnceAsync`/`ToggleContinuousAsync`/`StopAsync`，内部持有 `PipelineRunner`）；设备能力经**委托注入**（帧抓取工厂 `FrameProviderFactory` + 相机 IO 输出器 `CameraIoOutputOrNull` + 通信运行时工厂 `Func<ICommRuntime?>`（发送/接收数据节点用，未传=节点 ERROR），由 MainWindow 提供，服务不认识具体设备）。`Invalidate()` 标脏（`RefreshNodeTree` 尾部调用）、`HotApplyParam`（model_dir 标脏/其余 `ApplyNodeParam` 热更）、`SetNodeEnabled`（启停热切换）。`Completed/StateChanged/Failed` 后台线程回调，UI 自行调度。MainWindow 只剩按钮回调与结果订阅（`_execution` 字段；旧 `_runner/_pipeline/_pipelineDirty` 字段已删）。
- 结果回 UI 链：`PipelineRunner.Completed` → `PendingResultQueue`（纯逻辑有单测：`Publish/Take` 只留最新一轮、`DisposeSkipped` 容错释放被跳过结果的节点图）→ 渲染定时器 `RenderPendingResult` → `ShowExecutionResult`；Mat→BitmapSource 走 `UiExtensions.ConvertNodeImages`（按引用去重转换、转换完统一释放输入 Mat；`MatToBitmap` 同在 UiExtensions，原 MainWindow 私有实现已迁出）。

## 节点流水线（Detection/）

- 工具节点（Binarize/Geometry/**ColorTransform 颜色变换**/SaveImage/Display）不参与判定恒 OK；ColorTransform 转灰度（方式：加权平均[默认·OpenCV BGR2GRAY 同款]/算术平均[32F 核防饱和]/R/G/B 分量提取），输出单通道灰度图可直接供轮廓匹配/YOLO/Seg 消费（下游自带通道归一）。
- **图像源节点（`ImageSourceNode`）相机模式失败语义（2026-09-05 起）**：输入帧非空（生产硬触发）直接透传；输入为空（单次/连续执行）经 `FrameProvider`（MainWindow 注入，包 `CameraFrameGrabber`）同步抓帧——`CameraFrameGrabber.Grab` 在**硬触发模式下主动返回 null**（防干扰生产采集链路）、相机未连接/超时同样 null。取不到帧时节点**自判 ERROR 停线**（不再 OK+空图静默），Error 文案给出可操作提示（未连接→打开相机管理；硬触发→切连续/软触发或换图像文件模式）。文件模式取不到图仍是 OK+空（序列尾保持现状）。**Grab 的连续模式语义（2026-09-05 修）**：未预览+连续模式=临时 `StartPreviewAsync` 开自由采集等第一帧再 `StopPreviewAsync`（**不再临时切软触发**——旧实现 Start/StopGrabbing+TriggerMode/Source 寄存器频繁翻面导致取不到帧）；预览中只等帧；软触发模式直接 `SoftTriggerAsync`。**`SoftTriggerAsync` 短轮询化（2026-09-05 修，防 UI 卡死）**：取帧改为 50ms 短轮询（对齐 HardTriggerLoop），不得在持有 `_sync` 时 `GetImageBuffer` 长阻塞——UI 线程的 State/TriggerMode/IsPreviewing getter 都锁 `_sync`，长持锁会让 UpdateButtonState/按钮操作排队卡死。
- **按键控制节点（`KeyControlNode`，工具节点，恒 OK，2026-09-05 起）**：为检测流程绑定常用键盘键位（`trigger_key` 参数：空格/Enter/F5/F8/F12，默认空格）。**程序空闲时按下键位 = 执行一次完整检测流程**（图像源相机+软触发模式即软触发抓帧；文件模式推进序列）——相当于"软件替代 PLC 触发"的手动挡；连续执行/硬触发驱动检测中按键忽略（不打断）。绑定机制：MainWindow `_keyTriggerKeys` + `Window_PreviewKeyDown`（输入框/下拉框聚焦时不触发，防打字误跑流程；弹窗窗口聚焦天然不经过主窗口）；`RefreshKeyBindings()` 在 RefreshNodeTree 尾部 + HotApplyParam(trigger_key) 时重绑（集合不变跳过）。节点 Run 仅透传配置（无 OutputImage、`Values["trigger_key"]`），IsModelNode 排除（不进并发模型路径），键位解析 `ParseKey/KeyName` 纯函数有单测。
- **相机IO通信节点（自包含）**：IO 输出配置（输出线 Line1/2/3、Strobe 源、有效电平、持续时间）是**本节点参数**（随 recipe.json 保存，海康式模块弹窗），不再有共享 io_communication.json UI；参数面板含「测试输出脉冲（按当前参数）」按钮（与执行共用 `CameraIoNode.BuildOutputSettings`，生产运行中/相机未连接时拒绝）。设备侧触发输入（Line0/触发沿）属于相机管理弹窗的触发参数。执行链：`ctx.CameraIoOutput` 执行器由主窗口/生产服务注入，**相机未连接时主窗口传 null → 节点自判 ERROR（节点值含 error），不静默漏输出**；`PipelineRunContext` 已无 `CameraIoSettings`（共享 IO 配置穿线已删，`Pipeline.Run` 签名为 `Run(bgr, imageName, vmSource, cameraIoOutput, sourceOverrides, commRuntime)`）。strobe_source/output_when/save_mode 等选择项已中文化（等待触发帧/帧结束有效/曝光结束有效/定时器有效；输出类型 all→全部），旧配方英文值在 ShowChoiceCombo 兜底原样显示、运行端 Normalize 双向兼容。
- **发送数据/接收数据节点（VM 式工具节点，2026-09-07 起，镜像 VM 发送数据/接收数据模块）**：二者都不处理图像、不参与判定（IsModelNode 已排除，**漏加会进 RunNodeInputs 的并发模型阶段**）。**生产通信运行时 `Comm/CommRuntimeService`**（`ICommRuntime` 接缝：Send/Receive/IsHeld）：按设备名惰性建链（设备配置每次建链时从 comm.json 现读，弹窗改参数即时生效；断链 `LinkClosed` 后丢弃链路含积压、下次收发自动重建），每设备有界 FIFO 收包队列（容量 64，满丢最旧，Monitor 阻塞取）；链路工厂可注入（单测假链路）。注入链与相机 IO 同款：MainWindow 持 `_commRuntime` → `RecipeExecutionService` ctor `Func<ICommRuntime?>` 工厂 → PipelineRunner → `Pipeline.Run(..., commRuntime)` → `ctx.CommRuntime`；硬触发生产 `CameraInspectionService` 同样透传；`Window_Closed` 在停触发驱动之后、相机之前 Dispose。**发送数据节点**：参数 `device`（Kind="device" 下拉，选项实时读 comm.json 设备名）+ `send_text`（模板：`{节点名.键名}` 引用上游输出、`{decision}`=当前整线判定；未识别占位符置空+日志警告，`ResolveTemplate` 纯函数有单测；节点名含「.」按第一个「.」切分）+ `suffix`（附加结束符 无[默认]/\r\n/\n）。**接收数据节点**：`device` + `timeout_ms`（默认 5000；0=只取积压不等待；每次执行 FIFO 消费一条）+ `timeout_action`（**ERROR停线**[默认，VM 式]/按OK继续 received=0）。二者失败语义对齐 CameraIo：未选设备/设备不在 comm.json/运行时未注入/链路未就绪/收发失败 → ERROR 停线（error 值含可操作提示）；输出 sent/text/received/resolved_text/device（LabelMap 已译：已发送/文本/已收到/发送内容/通信设备；CharRec 整图 text 键同名，LabelMap 显示名统一为「文本」）。判定节点可直接引用 `接收数据1.text` 做条件（现有 Decision 机制天然支持）。**与通信管理弹窗互斥守卫**：弹窗 `ConnectAsync` 连接前查 `IsHeld` → 拒绝并提示设备被方案节点占用；反向（弹窗先连、后跑流程）端口冲突时节点报 ERROR 提示被占用——**已知限制：TCP客户端 同一设备被两边各自打开无端口冲突不报错**（语义上是两条连接，现场勿同时用）。测试：`CommRuntimeServiceTests`（假链路工厂）+ `CommDataNodeTests`（假运行时），全离线。
- **直线查找节点（`LineFindNode`，Type="LineFind"，2026-09-07 起，镜像 VM 卡尺找线 IMVSCaliperFindLineModu）**：**单搜索区域**语义（VM 同款，一个模块找一条线；own_rois 多框只取第一个+日志警告；无区域 → 自判 ERROR 停线但**透传源图**——显示区有图才能画框，旧实现提前抛异常导致永远无图可画的死循环已修），长轴=框的较长边（h>w 时角度+90 交换宽高）。算法链（`Detection/LineFind/LineFindCore.cs` 纯逻辑）：卡尺沿长轴均布（≥2）→ 法线方向 **0.5px 步长双线性采样**剖面（采样范围=卡尺搜索长度 ∩ 区域短边 ∩ 图像边界）→ 二项式高斯平滑（滤波尺寸 1=不滤波/3/5/7 px）→ 中心差分梯度（幅值刻度=每像素灰度变化，边缘阈值 0-255 同 VM 语义）→ 阈值+**边缘极性**（黑到白[默认·沿法线正向]/白到黑/任意）+**边缘类型**（最强[默认]/第一条/最后一条，选峰前先爬坡到局部 |梯度| 峰）→ 抛物线亚像素 → **全最小二乘**（2×2 协方差主方向）+ 剔除点数/剔除距离迭代剔离群（永不低于 2 点）→ 角度归一 (-90,90]。**输出**：x1/y1/x2/y2（直线端点，源图坐标）、angle、mid_x/mid_y、score（内点/卡尺数）、mean_contrast、count + **loc_x/loc_y/loc_angle/loc_valid 定位契约（中点+直线角度）——位置修正定位来源下拉已含直线查找**（`ShowPositionCorrectionEditor` 类型过滤 ContourMatch/FastMatch/LineFind）。判定：找到即OK[默认→未找到=NG，ContourMatch 同款]/找到即NG；有效点<2=未找到。**未找到诊断（2026-09-08 起）**：`LineFindCore.CaliperScanStats` 累积全部剖面最大梯度（阈值/极性过滤前），error 文案带 `采样半宽 ±Npx[（搜索长度 N 被 ROI 短边宽度 N 钳制）]｜边缘阈值 N｜剖面最大梯度 黑→白 N / 白→黑 N` + 分类建议（剖面全越出图像/几乎无灰度变化→检查区域或加大范围；梯度低于阈值→降阈值；过阈但极性不符→改极性；部分卡尺有边缘→检查区域覆盖），节点判 NG 时经 Log 进 UI 日志——"未找到"不再盲调。标注（矢量叠加层）：拟合线段+卡尺边缘点（AsPoints，绿/红按判定）+ 搜索区域四边形（黄）。**精度/性能**：0.5px 采样下绝对垂距 ±0.2px、角度 ±0.1°（合成 2px 过渡带标定）；理想硬阶跃边缘有 ±0.5px 相位歧义（真实镜头边缘有模糊不受影响）；**全链典型 <1ms，预算 5ms**——`LineFindTests.Performance_CoreUnder5ms/NodeUnder5ms` 是**常规测试集内**的中位数断言（1024×768+32卡尺×64 采样）。**拟合方式（2026-09-08 起，VM 对标）**：`fit_method` 鲁棒拟合[**默认**·VM 直线查找 FitFun=Huber 同款]/最小二乘[旧行为保留]——鲁棒 = LLS 初拟合 + **Huber IRLS**（`HuberWeights`：k=1.345×1.4826×MAD(|r|)，|r|>k 线性降权不丢点，下限 0.5px，≤8 轮至收敛，退化保持当前拟合；**只精修几何不改变内点集合**，离群点仍显示但权重↓）；旧 Tukey IRLS 实现保留在 `LineFindCore.TukeyWeights` 做 A/B；**已进位置修正目标勾选列表**（消费 `NodeRois.ApplyPoseCorrection`）。**与 VM 已知差异**：无。接线常规 4 件套 + **IsModelNode 排除** + ROI 绘制按钮清单（同轮廓匹配：只有绘制、无检测项管理）+ `FindDisplayedModelNode` 两处 + 定位来源过滤 + LabelMap（起点X/终点X/中点/平均对比度）。合成图单测 `LineFindTests` 25 例（含噪声重复性回归：30 帧噪声 Robust 中点 std≤0.15px）全离线。
- **圆查找节点（`CircleFindNode`，Type="CircleFind"，2026-09-08 起，镜像 VM 卡尺找圆 IMVSCaliperFindCircleModu）**：**单搜索圆环**语义（VM 同款，一个模块找一个圆；own_rings 多环只取第一个+日志警告；无环 → 自判 ERROR 停线但**透传源图**，同直线查找的画框死循环修复）。**搜索区域走独立 `own_rings` 节点参数**（圆环 ROI 类型，2026-09-08 起；own_rois 矩形检测项集完全不动）：条目格式 `名字;cx,cy,ro,ri`（`|` 分隔；圆心 cx 按图宽 cy 按图高归一化，**半径按图宽归一化**保证像素圆各向同性；`RoiRing.Parse/Serialize`，环宽钳制 ≥4px=剖面采样下限）。叠加层圆环分支（`RenderRings/LayoutRings/RingHitTest/ComputeDraggedRing/RingMouseDown/Move/Up/RightButtonUp`，当前显示节点是 CircleFind 时与矩形机制互斥）：拖拽画环=按下定圆心+拖距定外圆半径（内圆默认 0.6×外圆），选中后拖外圆柄/内圆柄（正东向两小方柄，RoiHandle.RingOuter/RingInner）调半径、拖内部移心、右键删除；写回经 `WriteOwnRings`（EnsureUnlocked 守卫已拦）热生效。算法链（`Detection/CircleFind/CircleFindCore.cs` 纯逻辑）：卡尺沿中圆 Rmid=(Ro+Ri)/2 角度均布（≥3）→ **径向** 0.5px 步长双线性采样剖面（采样范围=环带 [Ri,Ro] ∩ 图像边界，**无 search_length 参数——环带宽度即卡尺搜索范围**，VM 环形 ROI 同语义）→ 平滑/选边复用 `LineFindCore` 的 internal 静态（SampleProfile/SmoothBinomial/FindEdge[签名已解耦 LineFindParams]/ClampRange/SampleBilinear，零行为变化）→ 抛物线亚像素 → **Kasa 代数最小二乘圆拟合**（3×3 线性方程组闭式解，残差=径向距 |dist−R|）+ 剔除点数/剔除距离迭代剔离群（永不低于 3 点；近共线 → 未找到报"边缘点近似共线"）。**输出**：center_x/center_y/radius、score（内点/卡尺数）、mean_contrast、count + **loc_x/loc_y=圆心、loc_angle 恒 0（圆无方向，位置修正=纯平移）、loc_valid 定位契约——定位来源下拉已含圆查找**。判定：找到即OK[默认→未找到=NG]/找到即NG；有效点<3=未找到（未找到诊断信息同直线查找：`CaliperScanStats` + `BuildNotFoundMessage`，径向采样范围=Ri~Ropx）。标注（矢量叠加层）：拟合圆（72 段 Polygon，WPF 自动闭合，绿/红按判定）+ 卡尺边缘点（AsPoints）+ 搜索圆环内外双圆（黄 Info）。**性能**：核 0.115ms、节点全链 0.819ms（1024×768+32卡尺×65采样，中位数）——`CircleFindTests.Performance_CoreUnder5ms/NodeUnder5ms` 常规测试集内断言。**鲁棒拟合（2026-09-08 起，VM 对标）**：`fit_method` 同直线查找（**Huber IRLS** 作用于 Kasa 拟合，共享 `LineFindCore.HuberWeights`，旧 Tukey 的 `TukeyWeights` 保留做 A/B；默认=鲁棒拟合）；**已进位置修正目标勾选列表**（消费 `NodeRings.ApplyPoseCorrection` 环心映射，叠加层圆环分支画青色虚线修正环）。**与 VM 已知差异**：无。接线同直线查找全套 + LabelMap（圆心X/圆心Y/半径）+ ROI 名称列表/清除按钮按 own_rings 分叉（`ShowParamDefsInspector` ROI 区块内）。合成图单测 `CircleFindTests` 21 例 + `NodeRoisTests` 环形 3 例，全离线。
- **几何测量四节点（线线/线圆/圆圆/点圆，2026-09-08 起，镜像 VM 测量组 IMVSL2LMeasureModu/IMVSL2CMeasureModu/IMVSC2CMeasureModu/IMVSP2CMeasureModu）**：Type=`LineLineMeasure`/`LineCircleMeasure`/`CircleCircleMeasure`/`PointCircleMeasure`，共享基类 `MeasureNodeBase` + 纯几何核 `Detection/Measure/MeasureCore.cs`。**订阅模式（首版范围）**：线/圆/点引用上游节点输出值（条件检测同款 ctx.Results 机制），**VM「绘制」模式（模块内卡尺自找）不做**——找线/找圆用直线查找/圆查找节点；坐标系只做图像坐标系（特定坐标系/标定矩阵未做）。**引用契约**：线=直线查找 x1/y1/x2/y2；圆=圆查找 center_x/center_y/radius；点=定位节点 loc_x/loc_y（轮廓匹配/快速匹配/直线/圆查找均有，loc_valid≠1 → ERROR）。引用下拉是新 ParamDef Kind **`noderesult`**（`ParamDef.SourceTypes` 类型过滤，`ShowNodeResultCombo` 上游节点清单；引用被改名时原样追加显示、运行时报 ERROR）。**距离/角度语义（VM 用户手册实证）**：线线绝对距离=两线 4 端点各自到对线垂距的平均值（L2LAbsDist），夹角=θ2−θ1 视觉顺时针为正；线圆距离=圆心到直线垂距、垂足=垂线与直线交点、角度=垂足→圆心向量指向图像 y+ 为正；圆圆距离=两圆心连线长度、位置关系=内含/内切/相交/外切/外离（中文输出，等半径分离判相交/重合判内含）；点圆 中心距离=|点−圆心|、最近=中心−半径（点在圆内为负）、最远=中心+半径、圆圆/点圆角度=连线水平线下方为负（−atan2）。输出角度范围 `-180°~180°`[默认·VM 出厂]/`-90°~90°`（`NormalizeAngle`：Linear 归 (-180,180]，Segment 锐角折叠 mod 180）。**判定限全集内建**（VM 限值语义：每项独立 不判断[默认]/判断 + 下限/上限，含边界）：线线 距离/角度/交点X/交点Y 4 组；线圆 距离/角度/交点1X,1Y/交点2X,2Y/垂足X,Y 8 组；圆圆 距离/角度/交点1X,1Y/交点2X,2Y 6 组；点圆 中心/最近/最远/角度 4 组——启用项全过→OK 任一超限→NG（未过项记 `fail_checks` 中文清单）；交点不存在时交点判定启用即不过。**失败语义**：引用缺失/非上游/上游未找到线或圆（键缺失）→ **ERROR 停线**（Error 含可操作提示），与 CameraIo/发送数据节点同款。**纯几何不依赖图像**：底图为空不报错、不输出图像；底图非空时**透传底图作 OutputImage 作标注画布**（2026-09-08 修：原来完全不输出 → 无缩略图选不中该节点、矢量标注永远渲染不出来；标注坐标=上游几何的源图坐标，底图与上游源图同一图像源直供时对齐）（Pipeline 自动写 elapsed_ms/decision）；标注走矢量叠加层（被测线/圆 + 距离段 + 交点/垂足/圆心点，绿/红按判定）。点圆支持 按坐标手填点（point_x/point_y）与 按参数手填圆（circle_cx/cy/r）兜底模式。接线：NodeFactory Register ×4 + **IsModelNode 排除 ×4（基类 `MeasureNodeBase` 一个 case，漏加进并发模型阶段）** + NodeTypeLabels 中文名 ×4 + ShowParamDefsInspector case ×4 + LabelMap（绝对距离/夹角/交点/垂足/位置关系/中心距离/最近距离/最远距离等）。测试 `MeasureTests` 24 例（核心纯函数精确值 + 节点端到端 + 工厂往返 + ERROR 三例 + 全链 <1.5ms 中位数[含底图克隆]），全离线。

- 新增检测模型 = 实现 `IModelNode` + 在 `NodeFactory` 静态构造器 `Register` + 在 `MainWindow.NodeTypeLabels` 加中文名 + 在 `MainWindow.ShowParamDefsInspector` 的类型 switch 加 case（漏加则参数面板显示"暂无参数定义"，无法选模型）+ 让 `Pipeline.TryLoadModel` 覆盖新节点（漏加则模型永不加载）+ `Pipeline` 构造器 switch 注入 `Log`。MainWindow 另有**六处**按模型类型过滤易漏：`FindDisplayedModelNode`（ROI 叠加跟随）、ROI 编辑面板条件（「检测项管理」按钮）、`HotApplyParam` 的 model_dir 标脏、`ComputeCropRoiMismatch`（切图/ROI 失配校验：节点检测项名读 own_rois，sidecar 需含非空 `roi_name`——PatchCore/Seg/SemanticSeg/CharRec/Blob 都写该字段，YOLO 切图无 roi_name → 该节点校验静默跳过）、位置修正「要修正ROI的节点」下游勾选列表、`DefaultThresholdFor`（新建检测项预填阈值）。未注册类型运行时报 ERROR 不崩溃（`UnsupportedNode` 为通用扩展点占位）；旧类型自动映射：`ImageLoad` → `ImageSource`，`OCR`（旧占位）→ `CharRec`（字符识别）。
- 模型目录契约：PatchCore = `model.onnx + memory_bank.bin + backbone_config.json`；默认 `model_dir` 是 `..\..\..\..\patchcore train\models\foam_patchcore`（相对 exe 上溯 4 级到 speaker-inspection 根；`patchcore train/` 被 gitignore）。YOLO = `best.onnx + classes.txt`（`u训练` 导出产物）。Seg（实例分割）= 同 YOLO 契约，`u训练` yolo11*-seg 导出（output0 `[1,4+nc+32,N]` 检测+掩码系数 + output1 proto `[1,32,PH,PW]`；判定模式：缺陷占比≥阈值即NG[默认·`percent`%]/检出即NG/缺失即NG）。**SemanticSeg（语义分割）**= 同 YOLO 契约，`u训练` task=semantic（yolo26*-sem）导出，输出 `[1,H,W]` uint8 像素类别索引图（ParseClassMap 兼容 byte/long/int/float 与 [1,nc,H,W] argmax）；**输出值契约：nc>1 = argmax 索引即 classes.txt 行序；nc==1 = 二值图 {0=非该类(背景), 1=该类}**（ultralytics SemanticSegment head 导出时烘焙：`y.squeeze(1)>0`）——`RemapSingleClass` 把 nc==1 统一重映射为内部契约（行索引 + 255=忽略），漏掉这步会把类别像素当越界丢弃、把背景当缺陷（判定完全颠倒）；无实例框/置信度，按「关注类别」像素占比判定（判定模式与 Seg 同款复用 `SegNode.ComputeDecision`；classes.txt 中名为 背景/background 的类别永不算缺陷；ROI 检测项/启停/判断/阈值/存图/总范围/位置修正全部与 Seg 同机制）。**u训练 侧已知坑**：单类语义数据集（掩码 0=类、255=背景忽略）没有背景负样本 → 模型学成"处处是该类"（全图输出 1，ce_loss=0/mIoU=0/pixel_acc=1 指标失真），会导致 缺失即NG 全 NG/占比即NG 全 OK——u训练 已在 nc==1 时自动把掩码 {0=类,255=背景} 重映射为二值约定 {1=类,0=背景}，修复前训练的模型需重训；ContourMatch（轮廓匹配）= 模板目录 `shape_template.json`（建模弹窗生成：分层边缘方向点集+基准点；参数面板「创建模板（轮廓建模）」进 `ContourTemplateDialog`）；模板目录留空时自动用 方案目录/模板/节点名（海康式随方案管理，用户不必选目录）；**模板未加载 → 自判 ERROR 停线 + 透传源图**（2026-09-08 起：提前抛异常会让节点永远无图、没法画搜索 ROI 的死循环防御；建模弹窗用上游节点执行图像或文件，不依赖本节点先出图）；保存时同时写 `source_image.png` 存档，弹窗再次打开自动回填模板图/框选区域/基准点/建模参数（滤波Sigma/边缘阈值/金字塔层数）——调参→提取预览→保存即可更新模板，无需从头建模；弹窗支持「擦除点」（半径可调，各金字塔层按原图坐标同步擦除）与撤销/重做（快照深拷贝 50 步）；搜索侧平滑固定用模板存储 Sigma（节点无 sigma 参数，min_contrast 仅作用于搜索侧）；输出基准点位姿 `x/y/angle/score`，判定模式 匹配成功即OK[默认]/匹配成功即NG；算法核在 `Detection/ShapeMatch/`（纯逻辑，合成图位姿恢复有单测）。**完整模式提速（2026-09-06，结果逐位不变）**：粗层（有细化链兜底时）步长 1→2（量化 ≤1 粗层像素，细化收敛到同一连续峰值）+ 粗扫描按 角度×行带(~64px) 分块并行、细化按候选并行（分块分解与并行度无关、按块序合并——串行/并行逐位一致）+ ExtractDirections 方向归一化并行 + 双 Sobel 并发 + 金字塔流水线（层 i 方向提取与 pyrDown 下探重叠）+ **节点先 ROI 裁剪后转灰度**（大图小 ROI 省掉全图灰度转换）。实测泡棉场景 642×1071 ROI：585-736ms → **节点端到端 ~21-33ms**（真实帧新旧行为偏差 dx=dy=dAng=dScore=0.000，`FastMatchRealDiagTests` 锁定；合成精度标定报告全部在原包络内、确定性 0.00px）。两模式通用参数 `max_workers`（0=自动取 ProcessorCount-2，留 2 核给系统/UI；与其它模型节点并发时显式调低）。**⚠ 并行突发依赖线程池热度**：短时分块任务受 ThreadPool 缓慢注线程制约——应用启动已 `ThreadPool.SetMinThreads(ProcessorCount, ...)` 预热，冷启动的前一两帧仍可能偏慢。**AVX2 向量化评分（`ShapeMatcher.EnableAvx2Score`，默认关）**：8 点/批 gather+位掩码，索引钳制到数组内规避 vgather 掩码访存硬件差异，逐点算术与标量相同→结果一致（`Diag_Avx2VsScalar` 3420 样本锁定）；实测 AMD Zen4 上 vgather 慢于标量（并行场景 26→85ms），Intel 产线机可试开。**亚像素细化（2026-09-06 重写）**：最终候选在**连续评分面**上迭代坐标下降（模板点浮点变换 + 图像梯度场双线性插值，方向按最大幅值邻居 180° 符号对齐防对侧抵消；步长 1.0→0.5→0.25 三轮抛物线）——重复精度 0.19→0.00px，亚像素真值 0 偏差；粗细层搜索仍是整数最近邻（速度不受影响）。**实测精度**（`ShapeMatchAccuracyTests` 合成标定，SVI_ACCURACY=1 启用）：位置 ≤1.6px（恒定系统偏置，被「创建基准」吸收，产线只看相对偏移）、重复精度 0~0.5px（σ=20 强噪声 0.4~0.7px）、角度 ≤0.25° 中心区（搜索范围边角退化到 0.47°——工装角度行程贴边时放宽 angle_start/extent ±5°）、检出 100%（min_score=0.7）；**全图 1024×768 + 60° 角度范围搜索 ~1.7s/次，生产必须配 ROI 裁剪**（300×300 预计 150~400ms）。**FastMatch（快速匹配）**= 继承 ContourMatchNode（同 shape_template.json 模板契约/同 loc_* 输出契约/同判定模式/同「创建模板」入口，可与轮廓匹配共用建模），仅搜索内核换 `ShapeMatcher` 快速模式（fastMode=true）：**搜索金字塔向粗层多探层**（真实层先用满；模板层数不够时由最深层坐标减半**合成**缺层点集，仅作候选漏斗，最短边<48 停）+ **每角度预旋转模板点**（整数偏移+旋转梯度每角度算一次，粗扫描/细化位置循环复用；float 三角与原实现逐位一致，完整模式数值不变）+ 粗层位置步长 4（紧贴粗层的细化窗口 ±4，其下 ±2）+ 上层阈值放宽 0.2 + 候选漏斗（粗层 16 → 首层细化后 8）+ 每层点集均匀抽稀 ≤600 + 亚像素单轮抛物线 + **粗扫描按角度/细化按候选分块并行**（结果按索引序合并，与串行逐位一致；`max_workers` 参数 0=自动取 ProcessorCount/2，给并行模型节点/UI 留核）。**⚠ 采样激进度必须随模板半径自适应（真实场景踩坑）**：角度量化位移 ≈ 模板半径×sin(半步长)——native 步长链下 ≈ 半径/115 px（与层无关），角度倍步 ≈ 半径/57 px；位移过大粗层网格得分塌方、漏斗丢真值=「找不到模板」（半径 482px 的泡棉模板实测：倍步+合成全挂，关倍步+不合成 20ms 稳过；合成测试半径 86px 的小模板发现不了此坑）。规则：`FastMaxAngleDisplacementPx=2`，合成层仅当 半径/115≤2（半径≤~230px），倍步仅当 半径/57≤2（半径≤~114px）；旋钮 `FastMaxSynthLevels/FastDoubleAngleStep/FastCoarseStride/FastCoarseCandidates` 为 internal static（诊断测试 `FastMatchRealDiagTests` 用真实模板+原图逐项隔离，文件缺失静默跳过）。实测：泡棉真实场景 642×1071 ROI **~20-25ms**（完整模式优化后 ~25-33ms，见上），合成小模板 640×480 全图 ~8-20ms；目标 ≤0.5~1px/≤0.3~0.5°、粗定位节拍优先（重复精度 ~0.2px，绝对系统偏差被「创建基准」吸收），精度要求更高时换/加轮廓匹配；参数面板与轮廓匹配共用 `ContourMatchNode.StaticParamDefs`（含 max_workers，FastMatch 继承），其余接线同下；Pipeline 的 Log 注入/TryLoadModel 与 RecipeExecutionService 的 model_dir 标脏靠基类类型匹配自动覆盖派生类，但 NodeFactory/NodeTypeLabels/参数面板 switch/NodeEditor ROI 区块与定位来源列表/RoiOverlay 两处仍需显式列出。
- **字符识别节点（`CharRecNode`，传统视觉路线，2026-09-07 起；旧 OCR 占位节点经 NodeFactory 自动映射为 CharRec）**：检测项 ROI 内 二值化（固定阈值[默认]/Otsu + 极性 亮字暗底[默认]/暗字亮底）→ 膨胀/腐蚀（先膨胀后腐蚀，连接断裂/分离粘连）→ 连通域外轮廓字符分割（按 X 排序 + 高度过滤 min/max_char_h）→ 归一化 32×48 → 与字模库逐样本 Dice 相似度匹配（多样本取最高，未匹配 = `?`）。预处理链含 中值滤波 `median`（0=关/3/5/7，点阵喷码背景网点降噪——密集网点背景上 Otsu 阈值会漂高导致前景泛滥，此时改 固定阈值+中值滤波）；`show_binary`（显示二值预处理图，调参辅助——否则分割不出字符时画面无变化等于盲调）。**字模库契约**：`目录/字符/n.png`（归一化二值样本 PNG；`model_dir` 留空 = 方案目录/模板/节点名，海康式随方案管理；含路径非法字符的标签整串 URL 转义）；参数面板「字符训练（建字模）」进 `CharTemplateDialog`（样本图 = 上游节点执行图像或文件 → 框选单字符 → 输入标签 → 保存；预处理参数与节点共用，保存时自动回写节点热生效，保证训练/识别一致；字模列表可删除；**字模预览可擦除**——在归一化 32×48 预览上按住拖动擦掉噪点/邻字符侵入像素（所见即所得，擦的就是保存与匹配用的样本），半径可调、「撤销擦除」50 步，改任何预处理参数会重算样本并清掉手工擦除）。判定（`ComputeDecision`/`CheckPass` 纯函数有单测）：识别置信度（最弱字符 Dice）< 检测项阈值（缺省 0.7）→ NG；目标字符（RoiMeta 第5字段 `x=`，检测项管理「目标字符」列编辑）非空按判定方式 等于目标字符[默认]/包含目标字符 比对，不匹配/漏印(0字符)/未识别「?」→ NG；目标为空仅按置信度判定；「仅识别不判定」恒 OK（标注不染红，与 OK 判定一致）；启用且参与判定项任一 NG → 节点 NG。无检测项 = 整图识别不判定恒 OK（记一次日志提示画 ROI）。输出 text/count/conf + roi_名_text/conf/match（ModuleResultView 已译「检测项 名·识别文本/置信度/匹配」）；总范围按字符中心过滤；位置修正跟随、启停/仅观察/检测项阈值/存图全部与 Seg 同机制；识别区切图/整图留存同 YOLO 契约（crop_dir + save_mode；sidecar 含 roi_name/text/conf/target/count，文件名=时间戳_ROI名_文本_跨检测项全局编号（重名检测项不互覆），文本段剔除路径非法字符）。算法核在 `Detection/CharRec/`（纯逻辑，合成图端到端有单测 `CharRecTests`）。
- **Blob 分析节点（`BlobNode`，传统视觉路线，2026-09-07 起，镜像 VM「Blob查找/斑点分析」IMVSBlobFindModu）**：检测项 ROI 内 二值化（固定阈值[默认]/Otsu/双阈值/无二值化(输入已二值,非零即前景) + 极性 亮斑暗底[默认]/暗斑亮底）→ 孔洞填充 `hole_min_area`（外轮廓整体填充−原图取孔洞掩码，面积≤阈值填回；不用 hierarchy，规避嵌套岛误填）→ 连通域 `ConnectedComponentsWithStats`（8邻接[默认]/4邻接）→ 逐斑点特征（面积/周长/质心/最小外接矩形(minAreaRect)/长轴/短轴/圆度 4πA/P²[钳1]/矩形度 A/(w·h)[钳1]/灰度范围，掩码与轮廓只在外接矩形内取——大图小 ROI 省全图扫描）→ 特征筛选（面积[默认开 min=10]/周长/圆度/矩形度/长轴/短轴，各独立开关+min/max，VM SelectBy* 同款）→ 排序 `sort_feature`（面积[默认]/周长/圆度/矩形度/质心X/质心Y/外接框角度）×`sort_mode`（降序[默认]/升序/不排序）→ `find_num` 截取（VM FindNum 默认100）。判定（`ComputeDecision`/`CheckPass` 纯函数有单测）：检出即NG[默认]/缺失即NG/数量≥阈值即NG/最大面积≥阈值即NG/仅检出不判定；阈值=检测项级（`DefaultThreshold="1"`，数量与面积模式共用一个字段，语义随判定模式）。无检测项 = 整图当单一检测区照常判定（与 Seg 同、与 CharRec 不同）。无外部模型/模板（无 model_dir → 不进 TryLoadModel/HotApplyParam 标脏清单），参数全部热生效。输出 count/total_area/max_area + roi_名(数量)/roi_名_area(最大面积)/roi_名_total(总面积)（ModuleResultView 已译「检测项 名·最大面积/总面积」）；标注=斑点轮廓+面积/质心（检测项NG红/正常绿，整图模式也画；仅检出不判定不染红，与 OK 判定一致）+ ROI 四边形（黄）；`show_binary` 输出二值预览图（调参辅助，同 CharRec）；斑点区切图/整图留存/总范围(按斑点质心过滤)/位置修正跟随/启停/仅观察/存图 全部与 Seg 同机制（总范围 ROI 自身记得设「仅观察」，否则它罩住的斑会把自己判 NG）。算法核在 `Detection/Blob/BlobAnalyzer.cs`（纯逻辑，合成图有单测 `BlobNodeTests` 23 例；**不加** `NodeLifecycleTests` 的 LifecycleTypes——该测试断言 `model_dir` 存在且未加载即抛，Blob 均不满足，生命周期覆盖在 `BlobNodeTests.Factory_RoundTrip_Dispose`）。
- ROI 只存在于节点私有集 `own_rois`（**方案级 ROI 库已移除**：工具栏"ROI 库"按钮/继承模式/库引用链全部删除；旧配方里的 roi_mode/rois 参数残留不影响运行）。序列化格式：`name;cx,cy,w,h,angle[;e=..,d=..,t=..,s=..[,x=..]]`（第 3 段=检测项元数据 `RoiMeta`：e=启用 d=判断(空=参与判定/仅观察) t=检测项级阈值 s=存图 x=目标字符(URL转义，仅字符识别用)；缺省=全默认，旧配方零迁移兼容；`ParseOwnFull`/`SerializeOwnFull` 读写，`ParseOwn` 为几何投影）。UI = 参数面板「检测区域 (ROI)」绘制按钮（框挂到本节点；画框前节点须执行过一次——`ToggleRoiDraw` 有先后顺序守卫）+「检测项管理（本节点）」（仅 PatchCore/YOLO/Seg/SemanticSeg/CharRec/Blob；**轮廓匹配只有绘制，无检测项管理**）。检测项管理弹窗（`RoiManagerDialog`）：列表选中 ↔ 图像叠加层高亮双向同步、允许重名（全部按索引定位）、「重绘选中项」、「设为总范围」、以及检测项参数编辑（启用/判断/阈值/存图；CharRec 额外有目标字符列）。
- **检测项级参数（节点级总阈值已移除）**：YOLO `conf`、Seg/SemanticSeg `percent`、PatchCore `threshold` 参数行已删——阈值下沉到每个检测项（`RoiMeta.Threshold`，UI 新建时预填类型默认：YOLO/Seg/SemanticSeg 0.5/CharRec 0.7/PatchCore 模型训练阈值；缺省回退同款出厂默认）。YOLO 停用项不检测（roi_名=停用）、仅观察项检出记 `det_all` 但不进 triggered（不判 NG）、存图按检测项过滤；Seg/SemanticSeg 停用项占比=-1（输出停用）、判定=任一启用且参与判定项 占比≥该项阈值（`ComputeDecision(mode, roiChecks, triggeredCount)`；SemanticSeg 的 triggeredCount=有缺陷像素的启用判定项数）；PatchCore 同理（观察项 Decision="观察" 只记分，切图按检测项过滤）；CharRec 停用项不识别（roi_名=停用）。
- **⚠ OpenCvSharp 大坑：`Mat.GetArray` 返回托管拷贝**——写入数组后必须 `Mat.SetArray(buf)` 才会落回 Mat，否则全是无效写入（SemanticSegNode 曾因此缺陷掩码恒 0 → 语义分割全 NG）。全仓库只允许两种用法：GetArray **只读**，或写完**必须 SetArray**。已修：`SemanticSegNode.SegmentRegion` 的 contentMat/defectCrop/shifted 三处。
- **⚠ PowerShell 编码大坑（2026-09-06 实际事故）**：用 `Get-Content -Raw`（未显式 -Encoding，PS5.1 按 ANSI/GBK 读）+ `Set-Content` 改 UTF-8 源码 → 中文注释全体 mojibake、**多字节字符后的 `\n`/`<` 被当 GBK 双字节第二字节吞掉** → 语句被吞进注释（编译能过但语义丢失，如 `found.Add`/`if (_fastMode)` 消失=匹配全挂）。**改源码一律用 Edit/Write 工具，禁止 PowerShell 内容 cmdlet**。事故抢救方法：反向转码（当前文本按 GBK GetBytes → 按 UTF-8 GetString）恢复中文，被吞的换行/语句手工逐处恢复（扫描特征：一行内多个 `//`、注释行尾吞 `}`/代码），恢复后必须跑真实场景 A/B（`FastMatchRealDiagTests` 偏差 0.000）+ 全量测试验证语义完整性。
- **检测项名 ↔ 模型类名匹配**（`Detection/PositiveClasses.cs`，Yolo/Seg/SemanticSeg 共用，2026-09-04 起**「关注类别」positive_classes 参数已废弃删除**，旧配方残留该键不影响运行）：每个检测项（ROI）的名字与模型 classes.txt 类名做**忽略空格/大小写**匹配——命中 → 该检测项只判该类（像素/实例/检出过滤到该项 ROI 内）；未命中或无检测项 → 按全部非背景类别判定（背景/background 类名永不算缺陷），并对未命中的检测项名 WarnOnce 一次。改检测项名即可切换它关注的类别。
- **整图留存**（YOLO/Seg/SemanticSeg/CharRec 节点 `save_mode`，全部/仅OK/仅NG，默认 all）：按节点判定把原图存到 `crop_dir\OK|NG\时间戳_节点名.jpg`，与检测框/缺陷区/识别区切图共用 crop_dir；门槛逻辑统一在 `SaveImageNode.ShouldSave`（未知模式按 all 处理），SaveImageNode 自身的「保存类型」同源。注意：默认 all 意味着配置了 crop_dir 的旧方案升级后会开始留存整图。`crop_dir`/`dir` 未配置时各节点输出明确日志（Yolo/Seg/SaveImage 每帧一条，PatchCore 在 Run 里每帧只记一条防每 ROI 刷屏）。
- **总范围**（`scope_index` 隐藏参数，检测项管理「设为总范围」设置）：其余检测项结果只保留落在该 ROI 内的部分——YOLO 检出中心点判定（`YoloNode.ApplyScopeFilter` 纯函数）、Seg/SemanticSeg 缺陷掩码 AND 总范围（Seg 另按实例中心过滤）；删除总范围项时索引自动取消/平移。
- **位置修正**（`PositionCorrectionNode`，工具节点，海康式面板 `ShowPositionCorrectionEditor` **全引用化**）：读取定位节点的 `loc_x/loc_y/loc_angle/loc_valid` **标准契约键**（轮廓匹配已输出；后续新增定位手段输出同键即可接入，无需改位置修正）。定位来源=上游轮廓匹配**下拉引用**、修正目标=下游检测节点**勾选引用**（写入 target_nodes，不许手输名字；目标类型含 直线查找/圆查找，2026-09-08 起——直线查找经 `NodeRois.ApplyPoseCorrection` 消费、圆查找经 `NodeRings.ApplyPoseCorrection` 环心映射）；选择方式（按点/按坐标）仅切换引用行展示；X/Y方向尺度 `scale_x/scale_y`（默认1，变换 p′=R(Δθ)·S·(p−P0)+P1）；基准位姿 `base_x/base_y/base_angle` 由「创建基准」按钮从定位来源最近一次执行一键采集（VM 同名按钮；base_* 为 hidden 参数不手填）；图像显示 开关（`show_base/show_run`）控制结果图上基准点/运行点十字标注；变换写入 `ctx.PoseCorrection` 后目标节点经 `NodeRois.ApplyPoseCorrection` 跟随工件平移+旋转；定位失败（loc_valid≠1）→ 节点 ERROR 停线防误检；未设基准 → 透传不修正；Pipeline 构建时校验 target 须在下游/定位源须在上游/target 引用已失效（改名或删除后残留旧名，面板勾选列表也看不到）三种情况（日志警告）；勾选区有灰字提示「仅勾选的节点会跟随修正」——**未勾选的下游节点不修正，这是海康 VM 同款显式语义，不是 bug**；参数面板叠加层对修正目标节点额外画**青色虚线预览**（`BuildPreviewCorrection` 按最近一次定位模拟；非绘制模式只显示修正后形状，选中项才显示原始形状+手柄；矩形=青色虚线框，圆环=青色虚线修正环 `_ringPreviewHits`，命中测试按修正后几何、编辑写回原几何）。
- **ROI 叠加层交互**（防误触）：非绘制模式也可编辑——点框第一下=只选中（高亮+手柄切换），已选中的框再按住才拖拽/缩放/旋转；点空白=取消选中；画新框仍需绘制模式；右键删除。
- `PipelineResult.NodeImages` / `DisplayImage` 的 `Mat` 所有权移交调用方，UI 侧负责释放（沿用 OpenCvSharp 严格 dispose 约定）。`DisplayImage` 优先指向最后一个检测节点的图像（多图像源流程里排在检测节点之后的图像源不会盖掉检测结果，无检测节点图像时回退最后一个有图节点）；ROI 叠加只在当前选中的是模型节点时显示该节点自己的/引用的 ROI，非模型节点（如图像源）不显示任何 ROI——不要恢复"兜底显示全部方案 ROI"的旧行为（会跨节点混淆）。
- 检测结果的框线/文字标注走 `NodeShape` 矢量叠加层（`NodeResult.Annotations` → `PipelineResult.NodeAnnotations` → MainWindow 按 UniformFit 映射渲染，线宽/字号屏幕常量）——**不要把框线/文字用 PutText/Rectangle 画进位图**（位图随显示缩放会变粗/发糊，图像分辨率差异大时无法兼顾）；位图只保留像素级内容（SegNode 掩码染色、PatchCore 热图）。
- **模块结果（海康式，每个节点模块都有）**：节点弹窗（`NodeParamDialog`）参数区末尾追加「模块结果」区（`ModuleResultView`，代码构建控件）：上半部结果树（结果名称|当前值：模块状态[由 decision/error 合成 1/0]、耗时(ms)、判定 + 各输出键中文名，键名翻译表在 `ModuleResultView.LabelMap`，未知键原样显示）+ 下半部「当前结果」「历史结果」两页签表格（历史=执行序号|时间|模块数据，倒序，上限 100 条）。数据链：`Pipeline` 每节点计时写 `Values["elapsed_ms"]`（Run/RunNodeInputs/并行段三处都有）→ `ShowExecutionResult` 把 `result.NodeValues` 按节点逐个记入 `_moduleHistory`（`ModuleResultHistory`，纯逻辑有单测；节点重命名经 `RenameNode` 迁移合并、新建/打开方案随 `ResetEditHistory` 清空、执行序号跨方案只增不复位）→ `_paramDialog/_roiDialog?.NotifyResultChanged()` 只刷新结果区（**不重建编辑器**，避免连续执行时打断参数输入焦点）；硬触发驱动检测结果经 `OnTriggerDrivenResult` 同样记入 `_moduleHistory`。新节点输出值想显示中文结果名：在 `LabelMap` 加映射即可（roi_名/match_i_字段 自动翻译）。

## 生产闭环（Production/）

- **状态（2026-09-06 已接线）：硬触发驱动检测自动生效，无独立生产模式开关**——相机触发模式切到「硬触发」且已连接即自动装配 `CameraInspectionService`（轮询 `EnsureTriggerDrivenState` 挂在渲染定时器上）：PLC 触发一次 → 检测一次 → CameraIo 节点按配置发波 → 结果经相机 IO 电平回传；触发模式切离硬触发/相机断开自动拆卸。装配复用 `RecipeExecutionService.PrepareAsync` 的同一 Pipeline 实例（模型只加载一份），失败后 5s 退避重试。
- **结果回传 PLC = 相机 IO 电平信号（PLC 不解析字符）**：方案内「相机IO通信」节点按 参数 发脉冲——输出类型 NG/OK/ERROR/全部 + 输出线 Line1/2/3 + 有效电平 高/低（如 NG→IO2 高电平、OK→IO1 高电平）。`IPlcClient` 接缝当前仅 `LoggingPlcClient`（把 READY/BUSY/OK/NG/ERROR 状态流转记日志，联调观察用，不连任何设备）；若确认 PLC 支持文本/Modbus 协议再按 `IPlcClient` 接缝扩展，勿把文本发送当默认假设。
- 互斥与清理：触发驱动进行中拒绝单次/连续执行/按键触发（提示切离硬触发）；硬触发期间仅禁用「添加模块」，节点参数仍可热更新，其他方案结构变更（新建/打开/删改/移动节点）需确认后先安全停止触发驱动再执行，避免边检测边替换流程；`Window_Closed` 先停触发驱动再释放相机。`StatusTrigger` 状态栏显示触发溢出计数，状态灯显示 触发待机/检测中/触发错误（`TriggerState`）。
- `CameraInspectionService`：硬触发帧 → 有界队列（容量 1，`DropWrite`，溢出计入 `TriggerStateMachine.OverrunCount`，不无限排队）→ 流水线检测 → OK/NG → 相机 IO 电平脉冲（节点级配置）+ 状态日志。帧缓冲在相机事件线程内同步拷贝后立即释放——不要把 SDK 回调改成异步持有 `Mat`。

## 测试注意

- `RealModelTests` 在模型文件缺失时静默 `return`：绿色不证明数值一致性；要在真机模型上复跑。已接真机模型：PatchCore（`patchcore train\models\foam_patchcore`）与 SemanticSeg（`u训练\outputs\_test\e2e_semantic_run\weights`，端到端真实推理）。
- 相机/PLC 均为假实现注入（`CameraIoNodeTests`、`ImageSourceNode` 相机分支），不需要真实硬件。纯逻辑/编排层单测：`RecipeEditHistoryTests`（撤销/重做）、`PendingResultQueueTests`（跳帧释放 + Mat 批量转换）、`RecipeExecutionServiceTests`（执行编排：标脏重建/热应用/连续启停）、`CameraInspectionServiceTests`（假相机全链：硬触发帧→检测→PLC 状态日志→IO 发波）。
- 精度标定实验默认跳过：`ShapeMatchAccuracyTests`（轮廓匹配误差统计，~1m40s）需环境变量 `SVI_ACCURACY=1` 才跑，报告落盘 `%TEMP%\svi_shape_accuracy.txt`；`ShapeMatchBiasDiagTests` 打印偏差向量（恒定向量=系统偏差，随机=量化）——上机遇到可疑偏差先跑它定位。
- `FastMatchRealDiagTests`（真实场景诊断，文件缺失静默 `return`）：用泡棉方案的真实模板+原图（`bin\...\模板\08 快速匹配` + `Desktop\原图\1`）锁定三件事——①新旧实现 A/B 偏差 0.000（效果不变的证据）②AVX2≡标量（`Diag_Avx2VsScalar` 3420 样本）③真值位姿评分探针。匹配行为可疑时先跑它；`ShapeMatcher` 的调优旋钮（`FastMaxSynthLevels/FastDoubleAngleStep/FastCoarseStride/FastCoarseCandidates/FullCoarseStride2/EnableAvx2Score`）均为 internal static，诊断测试按 `[Collection("FastMatch")]` 与其它快速匹配测试串行调整。

## Git

- 已首次提交（`79d3b91`，含项目 `.gitignore`：忽略 `bin/ obj/ .vs *.user results/ appsettings.json`；检测结果 JSON 落在 `results/`，日志在 bin 输出目录 `logs/` 随 bin 忽略）。`启动视觉检测平台.lnk` 由父级 `.gitignore` 的 `*.lnk` 覆盖。
- git 仓库根在 `D:\AiProjects`（伞形工作区），`git add` 只加 `speaker-inspection/` 下的显式路径。
